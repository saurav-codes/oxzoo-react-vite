# Reference only. ox reads env from the dashboard Environment editor, not from this file.
# Set GREETING_TAG in ox BEFORE the first deploy (build-time for the SPA, runtime for the API).
GREETING_TAG=change-me
